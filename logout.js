import React from 'react';

const Logout = () => {
    return (
        <div>
            <h1>
                Welcome <span className='user_name'>Robi</span>
                <button className="logout_button">Logout</button>
            </h1>
        </div>
    );
};

export default Logout;
